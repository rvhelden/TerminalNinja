<Border xmlns="http://schemas.terminalninja.dev/xaml"
        BorderStyle="Rounded">
    <StackPanel Orientation="Vertical">
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1"
                   Text=" ComboBox" Padding="2,0,0,0" />
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="2"
                   Text="  Dropdown selection with inline items and data binding."
                   Padding="2,0,0,0" VerticalTextAlignment="Center" />

        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1" Text="" />

        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1"
                   Text="  Inline ComboBoxItems:" Padding="2,0,0,0" />
        <ComboBox StackPanel.SizeMode="Fixed" StackPanel.FixedSize="3" TabIndex="0">
            <ComboBoxItem Content="Red" />
            <ComboBoxItem Content="Green" />
            <ComboBoxItem Content="Blue" />
            <ComboBoxItem Content="Yellow" />
        </ComboBox>

        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1" Text="" />

        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1"
                   Text="  Bound ItemsSource:" Padding="2,0,0,0" />
        <ComboBox StackPanel.SizeMode="Fixed" StackPanel.FixedSize="3" TabIndex="1"
                  ItemsSource="{Binding Fonts}"
                  SelectedItem="{Binding SelectedFont, Mode=TwoWay}" />
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1"
                   Text="{Binding FontStatus}" Padding="2,0,0,0" />

        <TextBlock StackPanel.SizeMode="Stretch" Text="" />
    </StackPanel>
</Border>
