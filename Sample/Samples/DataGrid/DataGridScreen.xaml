<Border xmlns="http://schemas.terminalninja.dev/xaml"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dataGrid="clr-namespace:Sample.Samples.DataGrid"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=dataGrid:DataGridViewModel}"
        BorderStyle="Rounded">
    <StackPanel Orientation="Vertical">
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1"
                   Text=" DataGrid" Padding="2,0,0,0" />
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="2"
                   Text="  Multi-column grid with typed columns, sorting, and row selection."
                   Padding="2,0,0,0" VerticalTextAlignment="Center" />
        <TextBlock StackPanel.SizeMode="Fixed" StackPanel.FixedSize="1" Text="" />

        <DataGrid StackPanel.SizeMode="Stretch" TabIndex="0" ItemsSource="{Binding Employees}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="Id" Width="6" />
                <DataGridTextColumn Header="Name" Binding="Name" />
                <DataGridTextColumn Header="Department" Binding="Department" />
                <DataGridCheckBoxColumn Header="Active" Binding="Active" Width="10" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Border>
